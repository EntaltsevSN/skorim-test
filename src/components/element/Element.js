import styled from 'styled-components';

const Element = styled.div`
  padding: 32px;
  background-image: linear-gradient(135deg, #fff 0%, #c1d8f0 100%);
  box-shadow: 0 0 16px rgba(100, 100, 100, .4);
  border-radius: 8px;
  position: relative;
`

export default Element;